{
  "name": "@caveman-mem/mcp-server",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "bin": {
    "caveman-mem-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsup src/server.ts --format esm --dts --clean",
    "dev": "tsup src/server.ts --format esm --dts --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@caveman-mem/compress": "workspace:*",
    "@caveman-mem/config": "workspace:*",
    "@caveman-mem/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
