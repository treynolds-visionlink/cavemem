# Commit

refactor: clean up the authentication middleware and the configuration loader

The implementation was really starting to spread across multiple files and the documentation was out of date. I basically consolidated everything into a single module and updated the README to point to the new location. Note that the dependencies were also updated.
